


// Friend Discount
const React = require( 'react' );
class FriendDiscount extends React.Component{

    // Render
    render() {
        return (
            <div className="friendDiscount">
                <p>500kr rabat, hvis du tager en ven med!</p>
            </div>
        );
    }

} module.exports = FriendDiscount;
